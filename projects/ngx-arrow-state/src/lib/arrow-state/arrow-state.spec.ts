import { ArrowState } from './arrow-state';

describe('ArrowState', () => {
  it('should create an instance', () => {
    const directive = new ArrowState();
    expect(directive).toBeTruthy();
  });
});
