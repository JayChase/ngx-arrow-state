import { SubmitOnCtrlEnter } from './submit-on-ctrl-enter';

describe('SubmitOnEnter', () => {
  it('should create an instance', () => {
    const directive = new SubmitOnCtrlEnter();
    expect(directive).toBeTruthy();
  });
});
